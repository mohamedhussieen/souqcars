server {
    listen 80;
    server_name dashboard.yourdomain.com;
    root /var/www/sayarat-dashboard/dist/dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
